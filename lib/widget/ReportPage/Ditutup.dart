import 'package:flutter/material.dart';

class DitutupWidget extends StatelessWidget {
  const DitutupWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}